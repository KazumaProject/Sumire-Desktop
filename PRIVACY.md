# Privacy Policy

最終更新日: 2026-04-15

このプライバシーポリシーは、Sumire Desktop / Sumire IME（以下「本ソフトウェア」）が取り扱う情報と、その利用方法を説明します。

## 概要

本ソフトウェアは Windows 向けの日本語入力 IME です。入力変換は利用者の端末上で処理されます。本ソフトウェアは、テレメトリ、広告、行動分析、利用状況解析のための情報収集を行いません。

## 取り扱う情報

本ソフトウェアは、機能提供のために次の情報を端末上で取り扱います。

- 入力中の読み、未確定文字列、変換候補、左側の文脈など、IME の変換処理に必要な一時的なテキスト情報
- ライブ変換、候補数、表示言語、ローマ字変換表、zenz 補完、モデルファイル、モデル repo などの設定情報
- ユーザー辞書プロファイルの名前、元 TXT ファイルのパス、ビルド済み辞書ファイルのパス、有効または無効の状態
- インストール先、アンインストール情報、ショートカット作成に必要な情報
- インストール処理の成功または失敗を記録する簡易ログ

## 入力テキストの扱い

入力中のテキストは、変換候補を生成するために本ソフトウェア内で処理されます。`SumireZenzService.exe` を有効にしている場合、読みと左側の文脈は、同じ端末上のローカル名前付きパイプを通じて補助プロセスへ渡されます。

本ソフトウェアは、入力中のテキスト、変換履歴、確定文字列を、開発者のサーバーへ送信しません。また、これらを本ソフトウェア独自の履歴データベースとして保存しません。

## 保存場所

設定情報は主に次の Windows レジストリに保存されます。

```text
HKCU\Software\Sumire\Settings
HKCU\Software\Sumire
HKCU\Software\Microsoft\Windows\CurrentVersion\Uninstall\SumireIME
```

本ソフトウェアのファイル、モデル、辞書、インストールログは、既定では次の場所に保存されます。

```text
%LOCALAPPDATA%\SumireIME
```

ユーザー辞書を利用する場合、元 TXT ファイルの場所とビルド済み辞書ファイルの場所が設定として保存されます。辞書の内容は、利用者が選択したファイルと端末上に生成される辞書ファイルに含まれます。

## 通信

本ソフトウェアは、通常の入力変換のために外部サーバーへ通信しません。

ただし、インストール時に「既定の zenz モデルをダウンロードする」オプションを有効にした場合、モデルファイルを取得するために既定の Hugging Face repo へ接続します。設定でモデル repo や直接 URL を変更した場合、その接続先からモデルファイルを取得することがあります。

モデルダウンロード時には、接続先サービスやネットワーク事業者に対して、IP アドレス、アクセス日時、要求 URL、User-Agent など、通常の HTTP/HTTPS 通信に伴う情報が送信される場合があります。入力中のテキストやユーザー辞書の内容は、モデルダウンロードのために送信されません。

## 第三者サービス

既定の zenz モデル取得元として Hugging Face 上の repo を参照します。モデルをダウンロードする場合、接続先サービス側の利用規約およびプライバシーポリシーが適用されることがあります。

本ソフトウェアに同梱または参照されるサードパーティコンポーネントには、それぞれのライセンスや配布条件が適用されます。詳細は README と各コンポーネントのライセンスを確認してください。

## ログ

本ソフトウェアは、デバッグ出力として内部状態や処理結果を Windows のデバッグ出力へ書き出すことがあります。また、インストーラーは `%LOCALAPPDATA%\SumireIME\install.log` にインストール結果を記録することがあります。

これらのログは、変換履歴や入力内容を収集する目的では使用されません。ただし、利用者が外部のデバッグツール、ログ収集ツール、クラッシュ解析ツールなどを別途使用している場合、それらのツールが情報を取得する可能性があります。

## 削除

本ソフトウェアをアンインストールすると、インストールされたファイルとアンインストール情報の削除が試みられます。利用者が別途作成または指定したユーザー辞書の元 TXT ファイル、任意の場所に保存したモデルファイル、任意の場所に保存した設定用ファイルは、自動で削除されない場合があります。

設定や辞書ファイルを完全に削除したい場合は、アンインストール後に `%LOCALAPPDATA%\SumireIME` と、利用者が指定したユーザー辞書ファイルまたはモデルファイルを確認してください。

## 変更

本ポリシーは、本ソフトウェアの機能変更に応じて更新されることがあります。重要な変更がある場合は、リポジトリ内の文書またはリリース情報で告知します。

